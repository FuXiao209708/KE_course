现在已经有一个构建好的知识工程课程知识图谱，我需要基于存储在xxx的知识图谱构建一个问答系统，需要设计一个网页接受用户的问题输入，接受用户的问题输入后通过deepseek的api分析获得用户问题相关的关键词，通过关键词查询知识图谱，返回查询的结果以及一跳的邻居，将返回结果和用户的问题合并返回给deepseek的api，以生成自然语言的回答，在网页端生成给用户。


基于知识图谱中检索的结果作为外部知识用来给大语言模型构建提示。总体步骤如下：
1. 关键词提取：从用户问题中提取核心实体/概念作为KG检索的锚点。
提示词设计：请从以下用户问题中提取核心关键词（实体、概念、动作），要求：
1. 输出JSON格式，包含"entities"(实体)、"actions"(动作)、"attributes"(属性)三个字段
2. 实体需明确类型（人物/地点/产品等）
3. 保留原文本中的术语表达

2. 知识图谱检索：通过关键词定位KG中的相关子图。通过图数据库的Cypher查询实现关系扩展。图数据库为先前导入neo4j的知识图谱。

3. 提示词构建阶段：根据查询得到头实体、关系和尾实体或者属性值，将KG检索结果转化为自然语言上下文，拼接在用户的原始问题之后作为外部知识提示。要求大语言模型根据问题和外部知识生成自然语音回答。